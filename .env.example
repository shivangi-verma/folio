# Copy to .env.local for local development (used by server.js / vercel dev).
# In production, set these as Environment Variables in your Vercel project instead.
# This is a SECRET. Never commit it and never put it in client-side code.
GEMINI_API_KEY=your-gemini-api-key
GEMINI_MODEL=gemini-flash-lite-latest
